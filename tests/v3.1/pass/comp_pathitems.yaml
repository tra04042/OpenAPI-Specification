openapi: 3.1.0
info:
  title: API
  version: 1.0.0
components:
  pathItems: {}
openapi: 3.1.0
info:
  title: API
  version: 1.0.0
components:
  pathItems: {}
const fs = require('fs');
const yaml = require('js-yaml');

const fileContent = `
openapi: 3.1.0
info:
  title: API
  version: 1.0.0
components:
  pathItems: {}
`;

const parsedData = yaml.load(fileContent);
console.log(parsedData);
npm install -g @redocly/openapi-cli
openapi lint path/to/comp_pathitems.yaml
openapi-generator-cli generate -i path/to/comp_pathitems.yaml -g javascript -o ./output