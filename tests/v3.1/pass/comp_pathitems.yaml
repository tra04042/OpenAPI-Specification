openapi: 3.1.0
info:
  title: API
  version: 1.0.0
components:
  pathItems: {}
openapi: 3.1.0
info:
  title: API
  version: 1.0.0
components:
  pathItems: {}
const fs = require('fs');
const yaml = require('js-yaml');

const fileContent = `
openapi: 3.1.0
info:
  title: API
  version: 1.0.0
components:
  pathItems: {}
`;

const parsedData = yaml.load(fileContent);
console.log(parsedData);